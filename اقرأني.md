# تثبيت "مكتبتي" كتطبيق

هذه المجلدات تحوّل تطبيق مكتبتي إلى تطبيق قابل للتثبيت (PWA). لتفعيله لازم يكون على رابط إنترنت حقيقي (https) — فتحه من ملف على جهازك مباشرة ما يكفي لتفعيل التثبيت.

## الطريقة الأسهل والمجانية: GitHub Pages

1. أنشئ حساب على github.com (إذا ما عندك)
2. أنشئ مستودع (Repository) جديد، وارفع كل الملفات الموجودة في هذا المجلد (index.html, manifest.json, service-worker.js, icon-192.png, icon-512.png, icon-512-maskable.png) بنفس المستوى — بدون مجلدات فرعية
3. من إعدادات المستودع (Settings) → Pages → فعّل GitHub Pages من فرع main
4. بعد دقيقة أو دقيقتين بيعطيك رابط مثل:
   `https://username.github.io/repo-name/`

## تثبيت التطبيق على جوالك (أندرويد)

1. افتح الرابط من متصفح Chrome
2. بيظهر إشعار "إضافة إلى الشاشة الرئيسية" أو اضغط القائمة (⋮) → "تثبيت التطبيق"
3. التطبيق بيصير أيقونة على جوالك، يفتح بدون شريط المتصفح، ويشتغل حتى بدون إنترنت بعد أول فتحة

## تحويله لملف APK حقيقي (اختياري)

بعد ما يصير عندك الرابط من GitHub Pages:

1. روح لموقع **pwabuilder.com**
2. الصق رابط تطبيقك واضغط Start
3. اختر Android من الخيارات، واضغط Generate
4. بينزل لك ملف APK جاهز تقدر تثبته مباشرة على أي جوال أندرويد

ملاحظة: تخزين الكتب يبقى محليًا على كل جهاز تفتح منه التطبيق (ما يتزامن تلقائيًا بين الجوال والكمبيوتر).
